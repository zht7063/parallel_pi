# MVP 实施里程碑

2026-09-17。目标：完成既定 MVP，逐项验证 A01–A16；每完成一个里程碑进行一次 Git 提交。规格以 `mvp-spec.md` 为准，架构以 `architecture.md` 为准。Linux 是本次验收环境；macOS 按已有决定在 MVP 完成后实机验证。探针通过不替代应用验收。

| 里程碑 | 交付与退出条件 | 验收关联 | 状态 |
| --- | --- | --- | --- |
| M0 计划 | 完整范围、顺序与证据规则入库 | C01–C14、A01–A16 | 已完成 |
| M1 应用骨架 | npm workspace、TypeScript/Vue 构建、实际用到的分层包、依赖方向/公开入口/循环检查、领域独立测试、可启动服务；固定依赖 | I01；支撑全部验收 | 已完成 |
| M2 执行与恢复闭环 | SQLite 事务与事件、Git 身份/worktree、真实 pi 单次运行、图片持久化、模型固定、队列/停止/提问、进程归属与收束、pending 对账及明确恢复；最小真实 UI 贯通 | I02–I07；A01、A05–A11 | 已完成 |
| M3 地图与会话 | 全局地图/专注会话/地图浮层；预览与进入分离；独立/接续/fork；远端分支；多项目、草稿、视口与重连 | A02–A04、A08、A14、A16 | 已完成 |
| M4 配置与成果 | 原生模型/凭据配置及信任；MWF 初始化选择、召回、查看/修订、保存失败重试/明确继续；diff、整文件选择、提交与钩子失败；备份、macOS 本机 Linux 启动打包和手动升级说明 | A10–A13、A15 | 实施中 |
| M5 验收与交付 | 完整 A01–A16 证据矩阵、真实内核端到端、故障恢复、浏览器/键盘/窄窗口验证、安装运行文档；修复所有阻塞项 | A01–A16 | 待实施 |

## 提交与证据规则

- 每段完成后更新本表及实际证据，检查 diff，再提交该段代码、测试和文档。提交采用 Gitmoji + Conventional Commits。
- 不提交凭据、运行数据、缓存、依赖目录或临时测试产物。上游子模块保持固定版本。
- 只有检查命令实际通过才记为通过。真实 pi、可控 provider 与真实外部模型分别标明，不能相互替代。
- M2 中无法核验旧进程时保持 recovering；必须同时实现有证据的收束与对账恢复路径，不能以永久阻止代替恢复完成。
- 每阶段仅建立已经使用的能力；不为未来内核、同步、多用户或自动更新增加空实现。
- 最终验收逐项记录步骤、结果、证据路径和限制；未完成条目继续实施，不通过缩减范围关闭目标。

## 提交记录

- M0：本文件及文档入口；已核对 A01–A16、I01–I07 和当前干净工作区。

- M1：已建立 6 个实际使用的 workspace 包、Vue 连接界面、本地 HTTP 校验和纯领域规则。`npm run check` 通过（4 个测试）、`npm run build` 通过、Chromium 浏览器测试 1 项通过（连接、键盘重试、错误恢复、390px 窄窗口、滚动条）；中文字体补齐后截图已核对。UI 静态审查零问题，证据见 `evidence/m1-ui-audit.json`。DESIGN.md lint 为 0 errors、1 warning（现有纯 Markdown 无 YAML token frontmatter）。应用完整验收仍待 M2–M5。

- M2：执行与恢复闭环已通过 Linux 阶段检查：25 项 Node 测试、3 个浏览器场景、构建及 UI strict 审查。阶段证据见 `docs/evidence/m2-execution.md`；完整 MVP 仍待 M3–M5。

- M3：地图、接续/fork、远端分支、多项目/视口恢复与历史分页已完成；检查范围见 `evidence/m3-map-sessions.md`。

## M2 已完成：执行与恢复闭环

截至本次检查，正式应用已完成以下闭环；探针不计入这些测试数字。

- SQLite 状态与事件同事务保存；规范路径/真实第二实例排他、事务中 SIGKILL 回滚已有证据。单文档元数据是当前明确规模限制，事件单独存表。
- Git 规范仓库身份、dirty 保留、worktree 创建意图与实际产物对账已接入应用。错误绑定及进行中的 merge/rebase 阻止执行。
- Linux 监督采用 Python 3 标准库 + prctl subreaper + flock + pidfd。启动前记录 boot ID/PID/starttime，以 waitpid ECHILD 核验收束；恢复 tombstone 阻止延迟启动。正常退出、double-fork/setsid 工具停止、后端 SIGKILL、启动窗口恢复、PID 世代冲突已有真实进程测试。
- 受监督真实 pi RPC 已接入：文本、工具、提问、图片、原生会话持久化、精确模型校验、持久幂等入队、分支 FIFO/跨分支并行、停止后暂停及明确恢复。运行结果与 lane 状态同事务提交。
- 本地 HTTP 具备 Host/Origin/会话校验；快照不暴露原生会话引用，SSE 按持久游标通知。真实浏览器可完成添加项目、创建会话、图片发送、草稿刷新恢复、丢失确认后的幂等重试、提问和停止/恢复。
- 每次实际运行结束先原子保存运行结果和交接保存意图，再向应用私有目录发布不可变 JSON 文件。交接记录包含来源、观察到的文本及事件游标范围，不宣称它是经验证的长期结论或完整语义摘要。最多保存 20,000 字符的观察文本，截断显式标记；完整证据仍在事件日志与原生会话。
- 交接文件失败不会把成功执行改成失败；该列暂停，待写内容保留。重试相同内容核对已有文件并 fsync，不重跑模型；不同内容不覆盖。明确暂不保存保留标记和审计事件，然后仍需用户恢复队列。后端重启将未确认保存转为可见待处理状态，不能自动重放任务。
- 新增真实文件失败、已发布但未确认的恢复、冲突文件保护和明确继续测试；浏览器新增交接失败/刷新/重试/键盘/390px 操作可达性验证。长错误提示放在会话滚动区，避免挤掉窄窗口操作区域。

### 最新检查证据

- `npm run check`：25 个测试通过，架构依赖、类型与格式检查通过。
- `npm run build`：通过。
- `npm run test:browser`：3 个真实浏览器用例通过；执行环境需使用 `docs/development.md` 所述字体/运行库配置。
- UI strict 静态审查：0 errors、0 warnings；本次结果 `evidence/m2-ui-audit.json`。已查看 `test-results/handoff-failure-narrow.png`，按钮可达且没有固定错误提示挤压阅读区。

### 本阶段新增闭环与后续边界

- 固定版 MWF 的 add/propose 已由 infra-mwf 正式接入应用保存任务：先持久化内容，再启动受监督 CLI；同分支与 run 互斥，失败暂停，重复保存核对原请求回执。真实原生回执丢失确认、候选状态、保留 ignore 配置、其他 worktree 不隐式初始化、明确暂不保存均有应用测试。
- 浏览器可见长期记忆失败、刷新后仍保留、重试及明确暂不保存；初始化、召回、完整查看/纠正面板仍在 M4。不能据此关闭整个 A12。
- 已修复旧活动请求挂起阻塞新 run 的读取：切换 run 取消旧请求，测试用受控挂起稳定覆盖。已修复收束中先终止 sleep 唤醒其 shell 的竞争：RPC abort 是正常停止路径，监督器兜底按父到子强制终止，并继续以 ECHILD 为解锁证据。
- 用户已接受 macOS 通过本机 Linux 环境（Docker Desktop/Lima）运行后端与工具。容器启动、目录挂载和本机访问打包在 M4/M5 交付；macOS 实机验证按原决定后置，不宣称本轮已经验证 macOS。
- I02–I07 的阶段证据及后续义务见 [M2 退出证据](evidence/m2-execution.md)，固定版本入口与升级契约见 [原生依赖契约](native-contracts.md)。M3/M4/M5 的完整范围不变。

## M3 已完成：地图与会话

- 共享关系地图、全局与浮层独立视口、缩略图、预览/双击/显式进入、非模态 Esc 和焦点恢复均已接入；各项目恢复浏览会话与草稿，后台任务不随导航停止。
- 独立/接续/原生 fork 保持稳定身份及来源；首条和图片分叉保存为待编辑草稿，操作意图与不可变原生回执可恢复，不自动执行模型、不回滚代码。
- 本地建分支与远端 fetch/跟踪分支有真实 Git 测试和 pending 对账；无效名称可改正，缓存刷新失败保留旧列表，当前 dirty 内容不受影响。
- 历史采用 entry ID 分页；快照只带最近 40 条。阅读位置、旧消息分叉预览、加载失败重试已有验证，并修复加载提示消失引起的 43px 偏移。
- 阶段检查：30 项 Node 测试，4 个浏览器场景，构建与 UI strict 审查通过。具体范围、真实内核与 UI fixture 区分见 [M3 证据](evidence/m3-map-sessions.md)。
- 后续进入 M4 配置与成果、M5 完整验收。所有 C01–C14 / A01–A16 义务保持不变，当前不是完整 MVP。

## M4 实施中：全局设置首段

- 应用已接入原生全局 settings/auth：脱敏读取、API key 保存/移除、默认模型与原生锁内修订校验。未知字段保留，损坏原文不覆盖；不执行凭据中的命令表达式。
- 设置弹窗复用 AppDialog 和运行时 token，保存失败保留输入，显式重新读取后对照再保存；密钥仅保留于打开的组件，成功后清除。凭据移除和放弃未保存输入需明确确认。
- 新独立/接续表单读取项目覆盖或 pi 全局默认，不再从旧会话推断；历史分叉仍沿用源会话当前模型。项目原生配置、信任和模型目录接入尚待实施。
- 浏览器全套 5 场景通过；跨配置文件修订保护补充后，设置专场再次通过。已查看 390px 截图，表单可滚动且无横向溢出；UI strict 为 0 errors / 0 warnings，DESIGN lint 为 0 errors / 1 warning（既有纯 Markdown 格式）。
- 新增真实文件/HTTP 测试 2 项；阶段检查 `npm run check` 32 项通过，构建通过。完整 M4 尚未完成，尚未进行 M4 里程碑提交。
- 后续仍需 provider 连接/项目配置/信任、MWF 初始化与召回/纠正、diff/提交、备份及 Linux 打包；M5 全量验收范围不变。

### M4 工作区配置与原生模型查询进展

- 工作区默认模型写回各自的 `.pi/settings.json`，信任写回原生 `trust.json`，父目录继承、清除覆盖、修订冲突和未知字段保留已验证。
- 信任生效通过固定版原生服务/解析器在受监督 worker 中核对，保持全局扩展钩子的原生优先级。查询有分支持久化操作意图、维护互斥和恢复；不把文件策略预览当作真实运行信任结果。
- 原生模型目录查询、凭据可用性、视觉能力与共享选择器已接入；会话显式换模入口已补齐。实际浏览器验证了运行中/已排队模型冻结，另一个 worktree 未被隐式信任。
- 已修复显式恢复未纳入关闭等待的竞争，配置中断后不自动重载扩展。最终 `npm run check` 通过（36 项 Node 测试），构建通过，6 个浏览器场景通过；信任选择显示补充后专项再次通过。阶段证据见 `evidence/m4-configuration.md`，UI 审查见 `evidence/m4-config-ui-audit.json`。M4 尚未完成；此部分作为 M4a 阶段提交保存。
- 后续仍需 provider 自定义连接编辑、MWF 初始化/召回/纠正、diff/提交、备份和本机 Linux 打包。

### M4a 阶段提交：配置、信任与模型

- 将上述已验证的全局/工作区配置、原生信任、模型目录和会话换模独立提交，避免等待整个 M4 完成才保存进展。
- 复用已通过的 36 项 Node 测试、6 个浏览器场景、构建和 UI 审查证据；提交前再次检查差异格式。
- M4 其余范围保持不变，后续按可验证的完整功能段及时提交；本提交不代表 M4 或 MVP 完成。

### M4b 已完成：自定义 provider 连接

- 设置中可新增、编辑和移除原生 `models.json` 连接，选择三种常用 API 格式，并添加/更新模型及图片能力；原生模型目录读取到新模型。
- 原生 JSONC 与 schema 校验、锁内修订检查、未知字段/其他模型/内联认证保留均已覆盖；两个同修订并发写入只允许一个成功。损坏模型配置不阻断单独修复凭据。
- 37 项 Node 测试、架构/类型/格式检查和构建通过；连接与既有设置两个浏览器场景通过，选择器复位修正后连接专项再次通过。UI strict 为 0 errors / 0 warnings，已查看 390px 截图。
- 证据见 [连接编辑阶段记录](evidence/m4-connections.md)。本段单独提交；后续继续 MWF 初始化/召回/纠正、Git diff/提交、备份、本机 Linux 打包及 M5 完整验收。

### M4c 已完成：工作区记忆管理

- 分支地图接入记忆面板：明确初始化 track/ignore、原生分页查看/条件召回、来源与 scope、状态/正文纠正；已有 Git 策略和其他 worktree 保持独立。
- 修订比较、原生 update 和幂等回执共用原生项目锁/文件事务；应用持久化意图并协调分支维护，失败暂停，重启不自动重放，允许重试原修改或明确暂不保存。
- 修复了保存因未初始化失败后无法先初始化的恢复顺序，以及已接受意图但尚未登记子进程时重启留下永久 pending 的窗口；新增/纠正均有回归证据。
- 最终 `npm run check` 39 项 Node 测试、架构/类型/格式检查通过，构建通过；完整 8 个浏览器场景通过。最后补充非法 Git 策略保护后，原生记忆专项和架构检查再次通过。UI strict 0 errors / 0 warnings，DESIGN lint 0 errors / 1 项既有 warning。已查看 390px 截图。
- 证据见 [M4c 记忆管理](evidence/m4-memory.md)。此段单独提交；实际 agent 自动 bootstrap/MCP、Git 成果、备份/本机 Linux 打包/手动升级和 M5 完整验收继续实施。


### M4d 已完成：自动记忆与 MCP

- 真实 pi 运行按当前 worktree 自动加载原生 bootstrap/MCP；已有 adapter 复用，未初始化工作区不隐式创建记忆或安装项目资源。
- 兼容原生 bootstrap 去重；旧绝对路径扩展产生的外部记忆不进入当前模型上下文，原生历史保留。原生 MCP 服务拒绝跨根请求。
- 可见原生 custom message 进入持久运行通知，结束/刷新后可查；资源配置检查加载 eager MCP 后仍能完成受监督收束。
- `npm run check` 41 项 Node 测试与架构/类型/格式检查通过，构建通过；完整 8 个浏览器场景通过。UI strict 0 errors / 0 warnings，DESIGN lint 0 errors / 1 项既有 warning。已查看 390px 通知截图。
- 证据与范围见 [M4d 自动记忆](evidence/m4-memory-agent.md)。本段单独提交；后续继续 Git diff/整文件提交、备份/本机 Linux 打包/手动升级，以及完整 M5 验收。确定性 provider 的集成验证不替代外部模型应用验收。
